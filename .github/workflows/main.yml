name: 🌫️ Smoke Test Demo

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - name: 🧪 Check API is alive
        run: |
          echo "Testing JSONPlaceholder API..."
          curl -f https://this-site-does-not-exist-12345.com
